<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Wydarzenia</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">

    <style>
        body{
            background-color: #e2e2e2;
        }
        .login-form {
            width: 32%;
            margin: 50px auto;
            font-size: 15px;
        }
        .login-form form {
            margin-bottom: 15px;
            background: #f7f7f7;
            box-shadow: 0px 2px 2px rgba(0, 0, 0, 0.3);
            padding: 30px;
            border-radius: 3%;
        }
        .login-form h2 {
            margin: 0 0 15px;
        }
        .form-control, .btn {
            min-height: 38px;
            border-radius: 2px;
        }
        .btn {
            font-size: 15px;
            font-weight: bold;
        }
        .form-group{
            margin-bottom: 1rem;
        }
        table, th, td {
            border: 1px solid black;
        }
        table {
            border-collapse: collapse;
            width: 100%;
            margin-bottom: 4%;
        }
    </style>
</head>
<body>
<nav class="navbar navbar-dark bg-danger">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/home"><b>Gdzie Gramy?</b></a>
        </div>

        <ul class="nav navbar-nav navbar-right">
            <li><a class="text-white" href="/logout">Wyloguj</a></li>
        </ul>
    </div>
</nav>
<div class="login-form">
    <form action="showevent" method="post">
        <h2 class="text-center">Wydarzenia w których uczestniczyłeś: </h2>
        <div class="form-group">
            <table id="users">
                <tbody>
                <th> Nazwa wydarzenia</th>
                <th> Adres </th>
                <th> Czas rozpoczęcia </th>
                <th> Sport </th>
                <th> Więcej </th>
                {{#each events}}
                    <tr>
                        <td> {{this.name}} </td>
                        <td>{{this.court.adress.city}} {{this.court.adress.street}} {{this.court.adress.number}}</td>
                        <td>{{this.startTime}}</td>
                        <td>{{this.sport.name}}</td>
                        <td><button type="submit" class="btn btn-danger" name="EventID" value={{this.EventID}}>Szczegóły</button></td>
                    </tr>
                {{/each}}
                </tbody>
            </table>
        </div>

    </form>
</div>
</body>
</html>